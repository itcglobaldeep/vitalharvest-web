import { vi, describe, it, expect, beforeEach } from 'vitest'

describe('Feature Analysis', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('should analyze feature importance', async () => {
    // ... existing feature analysis test ...
  })
})